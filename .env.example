# 监听地址，格式为 ip:port，例如 :8080
LISTEN=127.0.0.1:8080
# 日志文件保存路径
LOG=/var/log/bepusdt/
# SQLite 数据库文件路径（如果使用 MySQL，此项可忽略）
SQLITE=/var/lib/bepusdt/sqlite.db
# MySQL 数据库连接字符串（留空则使用 SQLite）
# 格式：user:password@tcp(host:port)/database?charset=utf8mb4&parseTime=True&loc=Local
# MYSQL_DSN=root:password@tcp(127.0.0.1:3306)/bepusdt?charset=utf8mb4&parseTime=True&loc=Local
