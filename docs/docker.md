# Docker 部署

## 准备工作

1. **安装 Docker：** 确保服务器已经安装 Docker，这里不阐述安装细节。
2. **推荐服务器：** 推荐使用新加坡服务器(Debian12+)，性能无硬性要求，主要确保网络通畅；
   推荐尽量知名厂商(例如：Aws Gcp DigitalOcean 等)，一方面安全更有保障，其次一些私人VPS总是出现一些奇奇怪怪的问题！

## 部署命令

```bash
docker run -d --restart=unless-stopped -p 8080:8080 v03413/bepusdt:latest
```

### 说明：

- `-p 8080:8080` 表示将容器的 8080 端口映射到宿主机的 8080 端口，使得应用可以通过宿主机的端口访问。
- `--restart=unless-stopped` 确保你的容器在遇到问题时可以自动重启。
- 版本说明：`v03413/bepusdt:latest` 为最新发行版镜像，`v03413/bepusdt:nightly`为每日构建的开发版。

## 部署验证

确保命令执行无误后，控制台会返回一个容器 ID，此时执行命令 `docker logs -f [容器ID]` 你会看到类似日志：

```bash
#╔═══════════════════════════════════════════════════════════════════════
#║  🎉  欢迎使用 BEpusdt  -  首次运行检测，初始化配置完成
#╚═══════════════════════════════════════════════════════════════════════
#
#┏━━  🔐  后台登录信息 (请立即保存！)
#┃
#┃    👤  登录账号:  415e823251
#┃    🔑  登录密码:  31f6bf95bb
#┃    🛡️  安全入口:  /5d35710c90
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
#┏━━  🔌  API 对接信息
#┃
#┃    🎫  对接令牌:  43143d44bb616cb784b73a97394c5ffa
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
#⚠️   重要提示:
#    •  以上信息仅显示一次，请务必妥善保存至安全位置
#    •  登录密码遗忘可通过 'reset' 命令重置
#    •  API 令牌可在网页后台进行修改
#    •  建议定期更换密码以确保账户安全
#
#🚀  快速开始:  http://your-domain/5d35710c90
#
#═══════════════════════════════════════════════════════════════════════
#
```

日志中会显示初始化信息，包括后台登录入口 账号密码等，妥善保存；此时你可以通过访问`http://[你的IP]:8080/[安全入口]`进行后台登录。

## 忘记密码

执行命令重置：

```bash
docker exec -it [容器ID] bepusdt  reset
```

[‼️强烈推荐配置 TronGrid Api Key 提高 Tron 扫块稳定性](./tron-grid/readme.md)
---